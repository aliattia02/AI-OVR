Frontend test scaffold

Planned baseline coverage:
- auth session restore + refresh retry + redirect to /login on refresh failure
- incident workflow status transition request payload (`new_status`)
- patient submit flow (`/patients/submit/:uuid`) success/error handling

This repository currently has no frontend test runner configured.
