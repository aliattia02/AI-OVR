// frontend/src/components/shared/Navbar.jsx
//
// NOTE — Not currently rendered in the app.
//
// This file is intentionally kept as a placeholder for a future top navigation
// bar (app name + user badge). The current layout uses only the left-hand
// <Sidebar /> for navigation and role-aware links; a top bar was designed but
// not yet wired into <AppLayout /> in App.jsx.
//
// When the top bar is needed:
//   1. Implement the component here (app name, user avatar, notification bell).
//   2. Import it in App.jsx → AppLayout and render it above <Outlet />.
//   3. Adjust the <main> padding/margin to account for the bar's height.
//
// Do not delete this file — it is referenced in the component tree plan and
// will be activated in a future session.